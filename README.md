# مرشدك المهني 🎯

نظام تفاعلي شامل لاكتشاف الميول المهنية والمواهب باستخدام نموذج هولاند الشهير ونظرية الذكاءات المتعددة.

## 📊 نظرة عامة

**مرشدك المهني** هو تطبيق ويب احترافي يساعد الطلاب على:
- اكتشاف ميولهم المهنية وفق نموذج هولاند (RIASEC)
- تحديد مواهبهم الأساسية (7 أنواع من المواهب)
- الحصول على تقارير مخصصة مع توصيات للتخصصات الجامعية
- تصدير التقارير ومشاركتها

## 🌟 الميزات الرئيسية

### ✅ للطلاب:
- **استبيان تفاعلي** - 51 سؤالاً مقسمة على مرحلتين
- **تقرير شامل** - تحليل شخصي مفصل مع رمز هولاند
- **توصيات ذكية** - قائمة بالتخصصات المناسبة لكل طالب
- **قابل للطباعة** - تصدير التقرير بصيغة PDF
- **مشاركة سهلة** - رابط مباشر للتقرير

### 🎛️ للمرشدين:
- **لوحة تحكم احترافية** - إحصائيات وبيانات شاملة
- **تصدير البيانات** - CSV لتحليل جماعي
- **عرض التقارير** - جدول تفاعلي لجميع الطلاب
- **إحصائيات حية** - عدد المستخدمين والتقارير

## 🏗️ البنية التقنية

### Stack:
- **Backend**: Hono Framework + TypeScript
- **Database**: Cloudflare D1 (SQLite)
- **Frontend**: Vanilla JavaScript + TailwindCSS
- **Deployment**: Cloudflare Pages/Workers
- **Charts**: Chart.js (للرسوم البيانية)

### الملفات الرئيسية:
```
webapp/
├── src/
│   ├── index.tsx          # Entry point
│   ├── api.ts             # RESTful API
│   └── types.ts           # TypeScript types
├── public/static/
│   ├── app.js             # Frontend logic
│   └── style.css          # Custom styles
├── migrations/
│   └── 0001_initial_schema.sql
├── wrangler.jsonc         # Cloudflare config
└── ecosystem.config.cjs   # PM2 config
```

## 🚀 التشغيل المحلي

### 1. تثبيت المتطلبات:
```bash
npm install
```

### 2. إعداد قاعدة البيانات:
```bash
npm run db:migrate:local
```

### 3. بناء المشروع:
```bash
npm run build
```

### 4. تشغيل الخادم (للتطوير):
```bash
pm2 start ecosystem.config.cjs
```

### 5. اختبار:
```bash
curl http://localhost:3000
```

## 📊 قاعدة البيانات

### الجداول:
1. **starts** - سجل المستخدمين الذين بدأوا المقياس
2. **reports** - التقارير المنجزة مع النتائج

### Schema:
```sql
-- starts table
CREATE TABLE starts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  student_name TEXT NOT NULL,
  student_class TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- reports table  
CREATE TABLE reports (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  student_name TEXT NOT NULL,
  student_class TEXT,
  holland_code TEXT NOT NULL,
  holland_title TEXT,
  talent1 TEXT,
  talent2 TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## 📡 API Endpoints

### GET /api/stats
الحصول على إحصائيات عامة
```json
{
  "totalStarts": 150,
  "totalReports": 120,
  "hollandDistribution": [...],
  "talentDistribution": [...]
}
```

### GET /api/tables/reports
جلب التقارير مع pagination
```
Query params: page, limit, search
```

### POST /api/tables/reports
إنشاء تقرير جديد
```json
{
  "student_name": "أحمد محمد",
  "student_class": "ثالث ثانوي",
  "holland_code": "IS",
  "holland_title": "الباحث والاجتماعي",
  "talent1": "الموهبة الذهنية",
  "talent2": "الموهبة اللغوية"
}
```

### GET /api/tables/starts
جلب سجل البدايات

### POST /api/tables/starts
إنشاء سجل بداية جديد

## 🌐 النشر على Cloudflare Pages

### 1. إنشاء قاعدة البيانات:
```bash
npx wrangler d1 create webapp-production
```

### 2. تحديث wrangler.jsonc:
أضف database_id الذي حصلت عليه

### 3. تطبيق migrations:
```bash
npm run db:migrate:prod
```

### 4. النشر:
```bash
npm run deploy:prod
```

## 🎨 التخصيص

### الألوان:
يمكن تغيير الألوان في `style.css`:
```css
:root {
  --brand-1: #B4457F;
  --brand-2: #6D284F;
}
```

### البيانات:
- التخصصات: `AppData.majorCombinations` في `app.js`
- الأسئلة: `HOLLAND_QUESTIONS` و `TALENT_QUESTIONS`

## 📈 الإحصائيات الحالية

### الميزات المنجزة:
- ✅ نظام استبيان هولاند (30 سؤال)
- ✅ نظام استبيان المواهب (21 سؤال)
- ✅ قاعدة بيانات D1 كاملة
- ✅ API Backend RESTful
- ✅ لوحة تحكم احترافية
- ✅ تصدير CSV
- ✅ تقارير قابلة للطباعة
- ✅ مشاركة الروابط
- ✅ تصميم responsive

### قيد التطوير:
- ⏳ رسوم بيانية تفاعلية
- ⏳ تحليلات متقدمة بالذكاء الاصطناعي
- ⏳ نظام توصيات ذكي

## 🔐 الأمان

- جميع البيانات محفوظة في D1 Database
- CORS محدود للـ API
- لا توجد معلومات حساسة في Frontend
- التقارير مشفرة في الروابط

## 👨‍💼 المشرف

**د. عبدالغني النقيب**  
مرشد مهني - متخصص في التوجيه المهني والإرشاد الطلابي

## 📝 الترخيص

هذا المشروع مفتوح المصدر للأغراض التعليمية والبحثية.

## 🔗 الروابط المهمة

- **الموقع**: https://3000-ipfuqsrin4tsbsoxbbcxa-3844e1b6.sandbox.novita.ai
- **API**: /api/stats
- **لوحة التحكم**: الصفحة الرئيسية > "عرض لوحة التحكم"

## 🛠️ الدعم الفني

للأسئلة أو المشاكل التقنية، يرجى التواصل مع فريق التطوير.

---

**آخر تحديث**: نوفمبر 2025  
**الإصدار**: 1.0.0  
**الحالة**: ✅ نشط وجاهز للاستخدام
